Architecture Diagram

                                 +-------------------------+
                                 |      User Wallet        |
                                 |  (Naira/Fiat Payment)   |
                                 +-----------+-------------+
                                             |
                                             v
                           +--------------------------+
                           |    On-Ramp Provider      |
                           | (Naira → USDC on Solana) |
                           +-----------+--------------+
                                       |
                                       v
                   +------------------------------------------+
                   |     ChickenVault Smart Contract (Solana) |
                   |  • Holds USDC until delivery is confirmed|
                   |  • Maps Order ID to Farmer’s Wallet      |
                   |  • Emits audit events                    |
                   +-----------+------------------------------+
                               |
             +----------------+-------------------+
             |                                    |
             v                                    v
+-------------------------+       +-------------------------------+
| Delivery Oracle System  |       |        Off-Ramp Provider      |
| (Delivery confirmation) |       | (USDC → Naira to Farmer Bank) |
+-----------+-------------+       +-------------------------------+
            | 
            v
 +----------------------------+
 |   Trigger USDC release to |
 |   Farmer’s Solana wallet  |
 +----------------------------+


📜 PROTOCOL POC REQUIREMENTS FOR CHICKCHAIN
🔐 Admin + Configuration
• The protocol shall allow an admin wallet to set global config variables (vault timeout, oracle address, off-ramp limits).
• The protocol shall allow the admin to pause or resume vault operations in case of an emergency.
🛒 Order Creation & Vaulting
• The protocol shall allow a user to initiate an order by paying in Naira through a third-party on-ramp service.
• The protocol shall allow the system to receive USDC and associate it with a unique Order ID.
• The protocol shall vault the received USDC and hold it pending delivery confirmation.
• The protocol shall emit an on-chain event when vaulting is completed for transparency.
🚚 Delivery & Oracle Confirmation
• The protocol shall allow a designated oracle address or a multisig delivery authority to confirm delivery.
• The protocol shall allow manual or automated oracles to change the status of an order from PENDING to DELIVERED.
💸 Fund Release & Settlement
• The protocol shall allow the smart contract to release USDC from the vault to the farmer's wallet upon confirmation.
• The protocol shall emit on-chain proof of USDC transfer (including order ID, timestamp, farmer wallet).
• The protocol shall support multiple off-ramp providers for conversion of USDC to local fiat.
🔄 Refund / Reversal Mechanism
• The protocol shall allow users to reclaim funds if delivery is not confirmed within a set timeout (e.g., 72 hours).
• The protocol shall return USDC to the original sender’s wallet or initiate off-ramp refund flow.
📑 Traceability & Auditing
• The protocol shall store a hash of order metadata (e.g., chicken type, farm, timestamp) on-chain.
• The protocol shall allow third-party platforms to query on-chain order status and history.
💱 Token Conversion & Pricing
• The protocol shall interface with price oracles (e.g., Pyth) to verify current Naira–USDC rates.
• The protocol shall allow dynamic token pricing to reflect real-time FX rates during purchase.
⚙️ Instructions for Features Needed
🔧 Smart Contract Features
1. createOrder(order_id, farmer_wallet, amount_usdc)
    ◦ Initialize vault and map order to farmer
    ◦ Store order metadata hash
2. confirmDelivery(order_id)
    ◦ Oracle confirms delivery → updates status to DELIVERED
    ◦ Triggers USDC transfer to farmer wallet
3. releaseFunds(order_id)
    ◦ Executes transfer of funds to mapped farmer wallet if delivery confirmed
4. refundOrder(order_id)
    ◦ If timeout expires and delivery unconfirmed → refund USDC to user
5. getOrderStatus(order_id)
    ◦ Return full status (vaulted, delivered, refunded, expired)
6. setOracleAddress(addr)
    ◦ Admin function to update delivery oracle address
7. setOffRampProvider(provider_id, status)
    ◦ Admin-controlled function to enable/disable specific off-ramp services
8. setTimeoutPeriod(seconds)
    ◦ Admin sets timeout for order confirmation window
🧪 Off-Chain Modules
• Delivery Oracle Service
    ◦ GPS/QR triggered or manually submitted delivery verifications
    ◦ Pushes transaction to smart contract for delivery confirmation
• On-Ramp API Wrapper
    ◦ Converts Naira to USDC and transfers to Solana vault
• Off-Ramp API Wrapper
    ◦ After smart contract sends USDC to farmer, calls off-ramp API to auto-settle to bank account using pajcash
